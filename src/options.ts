import { IMinMaxPriceMarkersOptions } from './types';

// --------------------------------------------------
export const defaultOptions: IMinMaxPriceMarkersOptions = {
  textColor: '#000',
  zOrder: 'aboveSeries',
} as const;
